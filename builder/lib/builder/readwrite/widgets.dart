import 'package:flutter/material.dart';

class TheNiceNice extends StatelessWidget {
  const TheNiceNice({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    var tv1 = "";
    return Column(
      children: [
        // @text
        Text(tv1),
        // @end
      ],
    );
  }
}
 