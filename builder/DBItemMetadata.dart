//
// class DBItemMetadata {
//
//   String className;
//   // if its not, its a document, which means that is will found by its id
//   bool isCollection;
//   String path; // something like /Users/$authID/UserData(/$documentId)
//
// }