import 'package:flutter/material.dart';

class HowToContribute extends StatelessWidget {
  const HowToContribute({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("How to contribute");
  }
}
