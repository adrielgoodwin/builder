import 'package:flutter/material.dart';

class FoodGroupsApp extends StatelessWidget {
  const FoodGroupsApp({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("App");
  }
}
