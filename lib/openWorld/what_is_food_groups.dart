import 'package:flutter/material.dart';

class WhatIsFoodGroups extends StatelessWidget {
  const WhatIsFoodGroups({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("What is food groups?");
  }
}
